import 'package:flutter/material.dart';

class FavouritesFrame extends StatelessWidget {
  const FavouritesFrame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
