import 'package:flutter/material.dart';

class BreedsFrame extends StatelessWidget {
  const BreedsFrame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
